<% @messages.each do |message| %>
  <div class="row">
    <div class="col-sm-12 centered">
      <h3 class="message"><%= link_to 'To: ' + message.to, message_path(message) %></h3>
    </div>
  </div>
<% end %>
